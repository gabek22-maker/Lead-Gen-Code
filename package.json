{
  "name": "south-surrey-buyer-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.45",
    "tailwindcss": "^3.4.9",
    "vite": "^5.4.0"
  },
  "engines": {
    "node": ">=18.17"
  }
}
Click Commit changes.

2) (Optional safety) Make vite.config.js resilient
Still on GitHub, open vite.config.js → Edit and replace its contents with:

js
Copy code
import { defineConfig } from 'vite'

// Try to load the React plugin if it’s installed.
// This keeps builds from failing if the dependency wasn’t fetched yet.
let react = null
try {
  // top-level await is supported in Node 18+ (Vercel uses this)
  const mod = await import('@vitejs/plugin-react')
  react = mod.default()
} catch (e) {
  // falls back to no plugin (still builds basic React)
  console.warn('[@vitejs/plugin-react] not found – continuing without it.')
}

export default defineConfig({
  plugins: react ? [react] : [],
})
